# Canonical site URL used for SEO tags, JSON-LD, robots.txt, and sitemap.xml
# generation (scripts/generate-seo-files.mjs + src/config/site.ts).
# Falls back to a placeholder domain if unset.
VITE_SITE_URL=https://your-domain.example
